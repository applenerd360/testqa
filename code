
#include <iostream>

using namespace std;

string user_name;

int user_difficulty;

int main(int argc, const char * argv[]) {
   
    cout << "Hello there! What's your name?" << endl;
    getline(cin, user_name);
    cout << endl;
    cout << "So " << user_name << " what would you like your difficulty to be? 1 / 2 / 3" << endl;
    cin >> user_difficulty;
    if(user_difficulty == '1') {
        
        //Here come the difficulty 1 questions
        
    }
    
    else if(user_difficulty == '2') {
        
        //Here come the difficulty 2 questions
        
    }
    
    else if(user_difficulty == '3') {
        
        //Here come the difficulty 3 questions
        
    }
    return 0;
}
